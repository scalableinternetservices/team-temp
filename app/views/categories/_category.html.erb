<li id="question-<%= question.id %>">
  <%= link_to gravatar_for(question.id, size: 50), question.description %>
  <span class="timestamp">
    Posted <%= time_ago_in_words(question.created_at) %> ago.
  </span>
</li>